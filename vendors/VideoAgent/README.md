# VideoAgent

This is a git submodule placeholder. To initialize, run:
```bash
git submodule update --init --recursive
```

Main entry point: `python -m videoagent.run` (to be confirmed after submodule is cloned)
